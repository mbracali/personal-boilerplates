[project]
name = "docker-uv-htmx"
version = "0.1.0"
description = "Minimal FastAPI + HTMX boilerplate using uv."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.135.1",
    "uvicorn>=0.42.0",
    "jinja2>=3.1.4",
]

